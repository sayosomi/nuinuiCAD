// Temporary test-only bridge while DrawingCanvas integration tests migrate off the removed desktop host name.
// SAY-165 must delete this file before completion.
export { DrawingCanvasTestHost as TauriDrawingCanvas } from "./DrawingCanvas.testHost";
