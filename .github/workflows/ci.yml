name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  classification:
    name: Classify changes
    runs-on: ubuntu-24.04
    outputs:
      base_sha: ${{ steps.classify.outputs.base_sha }}
      node: ${{ steps.classify.outputs.node }}
      full_node: ${{ steps.classify.outputs.full_node }}
      vscode: ${{ steps.classify.outputs.vscode }}
      rust: ${{ steps.classify.outputs.rust }}
      parity: ${{ steps.classify.outputs.parity }}
      workflow: ${{ steps.classify.outputs.workflow }}
      unknown: ${{ steps.classify.outputs.unknown }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Test change classifier
        run: node --test scripts/ci/classifyChanges.test.mjs

      - name: Resolve diff base
        id: base
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_REF: ${{ github.ref }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          case "$EVENT_NAME:$EVENT_REF" in
            pull_request:*)
              base_sha="$PR_BASE_SHA"
              ;;
            push:refs/heads/main)
              base_sha="$PUSH_BEFORE_SHA"
              ;;
            *)
              base_sha=""
              ;;
          esac

          if [[ ! "$base_sha" =~ ^[0-9a-fA-F]{40}$ ]] ||
             [[ "$base_sha" =~ ^0+$ ]] ||
             [[ ! "$HEAD_SHA" =~ ^[0-9a-fA-F]{40}$ ]] ||
             ! git cat-file -e "${base_sha}^{commit}" 2>/dev/null ||
             ! git cat-file -e "${HEAD_SHA}^{commit}" 2>/dev/null; then
            echo "base_sha=" >> "$GITHUB_OUTPUT"
            echo "valid=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Unable to resolve a safe event diff base; using full CI coverage."
          else
            echo "base_sha=$base_sha" >> "$GITHUB_OUTPUT"
            echo "valid=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Check whitespace and classify changes
        id: classify
        env:
          BASE_SHA: ${{ steps.base.outputs.base_sha }}
          BASE_VALID: ${{ steps.base.outputs.valid }}
        run: |
          set -euo pipefail
          echo "base_sha=$BASE_SHA" >> "$GITHUB_OUTPUT"

          if [[ "$BASE_VALID" == "true" ]]; then
            git diff --check "$BASE_SHA" "$GITHUB_SHA"
            git diff --name-only -z "$BASE_SHA" "$GITHUB_SHA" |
              node scripts/ci/classifyChanges.mjs >> "$GITHUB_OUTPUT"
          else
            node scripts/ci/classifyChanges.mjs --full </dev/null >> "$GITHUB_OUTPUT"
          fi

  node:
    name: Node
    needs: classification
    if: ${{ needs.classification.result == 'success' && needs.classification.outputs.node == 'true' }}
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Full Node tests
        if: ${{ needs.classification.outputs.full_node == 'true' }}
        run: npm test

      - name: Changed Node tests
        if: ${{ needs.classification.outputs.full_node != 'true' }}
        env:
          BASE_SHA: ${{ needs.classification.outputs.base_sha }}
        run: npm test -- --changed "$BASE_SHA" --passWithNoTests

      - name: Build VS Code extension
        if: ${{ needs.classification.outputs.vscode == 'true' }}
        run: npm run build:vscode

  rust_parity:
    name: Rust + parity
    needs: classification
    if: ${{ needs.classification.result == 'success' && (needs.classification.outputs.rust == 'true' || needs.classification.outputs.parity == 'true') }}
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install Tauri Linux dependencies
        timeout-minutes: 10
        run: |
          sudo apt-get -o Acquire::Retries=3 update
          sudo apt-get -o Acquire::Retries=3 install -y \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Check Rust formatting
        if: ${{ needs.classification.outputs.rust == 'true' }}
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --check

      - name: Cargo check
        if: ${{ needs.classification.outputs.rust == 'true' }}
        run: cargo check --manifest-path src-tauri/Cargo.toml

      - name: Cargo test
        if: ${{ needs.classification.outputs.rust == 'true' }}
        run: cargo test --manifest-path src-tauri/Cargo.toml

      - name: Cargo clippy
        if: ${{ needs.classification.outputs.rust == 'true' }}
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings

      - name: Setup Node
        if: ${{ needs.classification.outputs.parity == 'true' }}
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install Node dependencies
        if: ${{ needs.classification.outputs.parity == 'true' }}
        run: npm ci

      - name: TypeScript/Rust parity
        if: ${{ needs.classification.outputs.parity == 'true' }}
        run: npm run test:parity

  ci:
    name: CI
    if: ${{ always() }}
    needs:
      - classification
      - node
      - rust_parity
    runs-on: ubuntu-24.04

    steps:
      - name: Check required CI results
        env:
          CLASSIFICATION_RESULT: ${{ needs.classification.result }}
          NODE_RESULT: ${{ needs.node.result }}
          RUST_PARITY_RESULT: ${{ needs.rust_parity.result }}
        run: |
          set -euo pipefail

          if [[ "$CLASSIFICATION_RESULT" != "success" ]]; then
            echo "Classification job did not succeed: $CLASSIFICATION_RESULT"
            exit 1
          fi

          if [[ "$NODE_RESULT" != "success" && "$NODE_RESULT" != "skipped" ]]; then
            echo "Node job did not succeed: $NODE_RESULT"
            exit 1
          fi

          if [[ "$RUST_PARITY_RESULT" != "success" && "$RUST_PARITY_RESULT" != "skipped" ]]; then
            echo "Rust/parity job did not succeed: $RUST_PARITY_RESULT"
            exit 1
          fi
